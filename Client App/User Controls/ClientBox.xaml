<UserControl x:Class="Client_App.User_Controls.ClientBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="200" MouseDown="UserControl_MouseDown">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Ellipse Width="40" Height="40" Grid.Column="0">
            <Ellipse.Fill>
                <ImageBrush x:Name="ProfileImage"/>
            </Ellipse.Fill>
        </Ellipse>
        <Grid Grid.Column="1">
            <TextBlock x:Name="ClientName" HorizontalAlignment="Center" VerticalAlignment="Center" Height="30" FontSize="15" FontWeight="DemiBold" Opacity="0.7" Width="150"/>
        </Grid>
    </Grid>
</UserControl>
